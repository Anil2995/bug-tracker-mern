# MongoDB Connection
# For local development:
MONGODB_URI=mongodb://localhost:27017/bug-tracker

# For MongoDB Atlas (Production) - Replace with your Atlas connection string:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bug-tracker?retryWrites=true&w=majority

# JWT Secret - IMPORTANT: Change this to a strong, unique secret in production!
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Port (Render will set this automatically)
PORT=5000

# Node Environment (development or production)
NODE_ENV=development

# Frontend URL for CORS (Required in production)
# Set this to your deployed frontend URL (e.g., Vercel or Netlify URL)
# FRONTEND_URL=https://your-frontend-app.vercel.app
